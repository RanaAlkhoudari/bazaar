This component;
    1.) interacts with  "http://localhost:5000/api/v1/products".
    2.) sorts the products by createdAt field which is in product.json.
    3.) shows the last 10 records from database.
    4.) uses productList component to render the last ten products.
